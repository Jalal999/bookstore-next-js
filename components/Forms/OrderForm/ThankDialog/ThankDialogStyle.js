import styled from 'styled-components';
import { Button, Dialog } from '@mui/material';

export const DialogLink = styled(Button)`
    display: block;
    border: solid 1px;
    padding: 10px;
    margin: 5px 5px;
`;

export const PopUp = styled(Dialog)`
    padding: 40px;
`
